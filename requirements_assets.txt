# Dependencies for the asset search pipeline
# Install with: pip install -r requirements_assets.txt

# Vision model (Gemini)
google-generativeai>=0.7.0

# Text embeddings (local, free — no API key)
sentence-transformers>=3.0.0

# Vector database (local, no Docker needed)
chromadb>=0.5.0

# FastAPI backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Image handling
Pillow>=10.0.0
